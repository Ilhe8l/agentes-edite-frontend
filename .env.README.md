# 🔧 Configuração de Ambiente

## Arquivos de Configuração

- **`.env`** - Arquivo ativo (usado pela aplicação)
- **`.env.development`** - Configuração para Django local
- **`.env.mock`** - Configuração para modo mock (offline)
- **`.env.example`** - Exemplo de configuração

## 🚀 Como Usar

### Modo Django (Padrão)

Conecta ao backend Django em `http://localhost:8000`:

```bash
npm run switch:django
npm run dev
```

### Modo Mock (Desenvolvimento Offline)

Usa dados simulados, não precisa do Django rodando:

```bash
npm run switch:mock
npm run dev
```

Ou diretamente:

```bash
npm run dev:mock
```

## 📝 Configuração Atual

O arquivo `.env` está configurado para:

```
VITE_API_BASE_URL=http://localhost:8000
VITE_USE_MOCK=false
```

Isso significa que o frontend está **conectado ao Django**.

## ✅ Verificar Conexão

1. Certifique-se que o Django está rodando em `http://localhost:8000`
2. Verifique que o CORS está configurado no Django para aceitar `http://localhost:3000`
3. Tente fazer login no frontend

## 🔍 Debug

Se o login não funcionar:

1. Abra o console do navegador (F12)
2. Vá na aba Network
3. Tente fazer login
4. Verifique se a requisição para `/api-token-auth/` foi feita
5. Veja o status code e a resposta

### Erros Comuns

- **CORS Error**: Django não está aceitando requisições do frontend
- **Network Error**: Django não está rodando
- **401 Unauthorized**: Credenciais inválidas
- **404 Not Found**: Endpoint não existe no Django

## 🔐 Endpoint de Login

O frontend faz POST para:

```
POST http://localhost:8000/api-token-auth/
Content-Type: application/json

{
  "username": "root8",
  "password": "root8"
}
```

**Importante:** O Django usa **username** (nome de usuário), não email!

Resposta esperada do Django:

```json
{
  "token": "abc123..."
}
```

## 📚 Mais Informações

Veja a documentação completa em:
- `ARCHITECTURE.md` - Arquitetura da integração
- `docs/DJANGO_INTEGRATION.md` - Detalhes da API Django
- `.kiro/specs/django-backend-integration/` - Especificação completa
